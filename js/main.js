"use strict";

// ==================== MAIN ==================================================
const main = (() => {
    window.addEventListener("load", function() {
      document.addEventListener("click", handlers.clickHandler, false );
    });
})();
